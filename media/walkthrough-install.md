## Install the Memanto CLI

Memanto is a Python command line tool. It needs Python 3.11 or newer.

```
pip install memanto
```

Other options: `uv tool install memanto` or `pipx install memanto`.

This extension never installs anything for you.
